{
  "scripts": {
    "build": "node build.js",
    "build:en": "node build.js en",
    "build:ja": "node build.js ja",
    "build:pages": "node build-pages.js"
  },
  "devDependencies": {
    "marked": "^9.0.0"
  }
}
